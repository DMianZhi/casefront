# AI 编程准则 — 7 维自查协议

与 AI 结对编程时强制执行的质量规范，目标是把质量内化到代码生成阶段，而不是事后返工审查。
来源：https://github.com/DMianZhi/AIforcode （MIT）

生成/修改任何代码后，必须严格执行以下 7 维自查，任一项不达标须在输出最终代码前自行修正，并在回复末尾附带《自查报告》。**DRY 与正确性为硬阻塞项**。

## 1. 正确性与健壮性
- 逻辑是否完全覆盖需求？边界条件（空值、零值、极大值、并发）是否处理？
- 错误处理是否具体？禁止吞异常或笼统 try-catch；错误信息须含上下文。
- 是否存在资源泄漏（文件句柄、DB 连接、内存未释放/关闭）？
- 输入验证是否充分？防御注入/XSS/路径遍历等。

## 2. 🚫 零容忍重复（DRY，优先级高于"快速实现"）
- **3 次法则强制**：相同/相似逻辑出现 ≥2 次即提取为函数/组件/工具类，不等第 3 次。
- **结构重复检测**：识别"结构相似但变量名不同"的块，用泛型/策略/配置驱动消除。
- **禁止防御性复制**：怕影响原逻辑而复制整个函数再微调 → 必须参数化/钩子/组合。
- **跨文件/跨模块扫描**：怀疑项目已有类似实现须先搜索/询问，禁止重新造轮子。
- **数据模型/类型定义**：DTO/Interface/Schema 用 extends/pick/omit/composition 复用，禁止手抄字段。
- **测试代码亦需 DRY**：setup/assertion/helper 重复提取为 fixture/helper。
- **合理豁免**：仅当两段代码语义本质不同、仅表面语法相似时可独立实现，须在自查报告说明理由。

## 3. 可读性与可维护性
- 命名精准、体现意图，避免 temp/data/info 模糊词；符合项目约定。
- 单一职责；单函数 ≤50 行，嵌套 ≤3 层。
- 注释解释 Why 非 What；复杂业务逻辑有文档注释。
- 格式统一（缩进/空格/换行符合主流风格）。

## 4. 性能与效率
- 复杂度最优？避免 O(n²)/重复计算。
- I/O 合理？避免循环内查库/请求网络，用批量或缓存。
- 内存克制？避免不必要的大对象拷贝/字符串拼接。
- 避免 N+1 查询。

## 5. 安全性
- 敏感数据（密码/密钥/Token）禁止硬编码 → 环境变量/配置中心。
- 用户输入清洗 + 参数化。
- 权限校验在入口处完成，无越权风险。
- 依赖项无已知高危漏洞。

## 6. 可测试性
- 核心逻辑与外部依赖解耦，便于 Mock/Stub。
- 明确输入输出契约，副作用可控。
- 关键路径提供单测示例/测试建议。

## 7. 工程规范
- 遵循现有架构模式与分层约定。
- 日志规范（级别正确、不打印敏感信息）。
- 类型注解/接口定义完整（TS/Python/Rust 等）。

## 自查报告格式（每次输出代码后必附）

```
✅ 自查通过项:  [通过的维度编号及名称]
🔍 DRY 扫描结果: [未发现重复 / 已消除的重复点 / 合理豁免理由]
⚠️ 已修正项:    [发现并已修复的问题]
❓ 待确认项:    [需人类决策的设计权衡或潜在风险]
💡 优化建议:    [非阻塞改进方向]
```
